services:
  postgres:
    image: postgres:16-alpine
    container_name: voryent_postgres
    environment:
      POSTGRES_USER: voryent_user
      POSTGRES_PASSWORD: voryent_password
      POSTGRES_DB: voryent_ai
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U voryent_user -d voryent_ai"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: voryent_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data: